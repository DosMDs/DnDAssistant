#Область ОбработчикиСобытий

// Ограничивает список выбора локаций по Кампании (Владелец).
//
// Платформа автоматически передает отбор по владельцу (Кампании) при выборе
// родительской локации из формы элемента справочника. Обработчик является
// точкой расширения для дополнительных ограничений и согласован с
// проверкой в модуле объекта: Кампания родителя должна совпадать с Кампанией
// редактируемой локации.
//
Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)

	Если Параметры.Отбор.Свойство("Владелец") Тогда
		Возврат;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти
